<h1><%= @user.username %> <%= @title %></h1>

<ul>
  <% @users.each do |user| %>
   <li><%= link_to "#{user.username}",user_path(id: user.id), class: 'navbar-link' %> </li>
  <% end %>
</ul>

<div class='paginate'><%= paginate @users %><div>

<%= link_to t('link.back'), :back %>
